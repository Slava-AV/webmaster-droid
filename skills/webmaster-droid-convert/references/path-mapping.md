# Path Mapping

Map content by semantic location:
1. Page-local content to `pages.<pageId>...`.
2. Shared navigation/footer/header to `layout...`.
3. SEO fields to `seo.<pageId>.title|description`.
4. Brand visual tokens to `themeTokens...`.

If mapping is ambiguous, use a TODO path and flag for manual review.
