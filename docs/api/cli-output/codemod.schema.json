{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://webmaster-droid.dev/schemas/cli-codemod.schema.json",
  "title": "CLI codemod --json response",
  "type": "object",
  "required": ["ok", "command", "version", "timestamp"],
  "properties": {
    "ok": { "type": "boolean" },
    "command": { "const": "codemod" },
    "version": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" },
    "data": {
      "type": "object",
      "required": ["reportPath", "source", "apply", "changedFiles"],
      "properties": {
        "reportPath": { "type": "string" },
        "source": { "type": "string" },
        "apply": { "type": "boolean" },
        "changedFiles": { "type": "integer", "minimum": 0 }
      },
      "additionalProperties": false
    },
    "errors": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1
    }
  },
  "allOf": [
    {
      "if": { "properties": { "ok": { "const": true } } },
      "then": { "required": ["data"] }
    },
    {
      "if": { "properties": { "ok": { "const": false } } },
      "then": { "required": ["errors"] }
    }
  ],
  "additionalProperties": false
}
